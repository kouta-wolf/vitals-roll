<%= turbo_stream.replace dom_id(@character, :round), partial: "characters/round", locals: { character: @character } %>
<%= turbo_stream.replace dom_id(@character, :buffs_list), partial: "buffs/list", locals: { character: @character } %>
