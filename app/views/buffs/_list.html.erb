<div id="<%= dom_id(character, :buffs_list) %>" class="space-y-1 mt-3">
  <%= render partial: "buffs/buff", collection: character.buffs, as: :buff, locals: { selected_weapon: local_assigns[:selected_weapon] } %>
</div>
