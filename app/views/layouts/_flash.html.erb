<% flash.each do |type, message| %>
  <div class="m-4 max-w-lg mx-auto rounded-md px-4 py-3 text-sm font-medium <%= type.to_s == "alert" ? "bg-red-50 border border-red-300 text-red-700" : "bg-green-50 border border-green-300 text-green-700" %>">
    <%= message %>
  </div>
<% end %>
